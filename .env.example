# ── Required for AI features (all degrade gracefully without) ──
OPENAI_API_KEY=sk-your-key-here          # AI summaries, chatbot, doctor chat, TTS

# ── Flask ──
SECRET_KEY=change-me-to-a-long-random-string

# ── Optional: live AI-doctor video (falls back to text + TTS without) ──
TAVUS_API_KEY=

# ── Optional: production database (SQLite used by default) ──
# DATABASE_URL=postgresql://user:pass@host:5432/caremate

# ── Optional: WhatsApp reminders ──
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=
